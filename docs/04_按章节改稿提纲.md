# 按章节改稿提纲（可直接执行）

## 使用说明

- 目标：把当前稿件从“工程报告”提升到“可投稿论文”。
- 原则：先做一致性修复，再做严谨性补强，最后做表达优化。
- 执行顺序：严格按本文档的 P0 → P1 → P2。

---

## P0：先统一口径（不做这个，后续改动都不稳）

1. 任务定义二选一并全局统一
- 方案 A：单类 drone 自建数据集论文。
- 方案 B：VisDrone 10 类公开数据集论文。
- 要求：摘要、引言、实验设置、表格、结论、README、脚本全部一致。

2. 方法定义二层化并统一
- 核心方法层：SA-NWD（loss/TAL/NMS，明确主版本与扩展版本）。
- 架构增强层：P2 + SimAM + PConv（与核心方法解耦叙述）。

3. 理论与实现一一映射
- 统一尺度符号：明确定义 `s` 是面积、边长或 `sqrt(wh)`。
- 统一 `C(s)` 形式：正文、附录、代码注释一致。
- 增加“公式到代码映射表”：公式参数 ↔ 代码超参数。

4. 清零占位与可疑文献
- 替换所有 `XX.X`、`待定`、占位结果。
- 清理不可核验引用（placeholder 风格条目必须替换）。

---

## 第一章：Abstract（摘要）改稿提纲

### 1. 必须保留
- 任务场景：UAV 小目标检测。
- 核心问题：小尺度下 IoU 系列监督信号不足。
- 方法主线：SA-NWD + 轻量结构增强。
- 结果主线：精度提升 + 参数/速度代价。

### 2. 必须删除或替换
- 未完成实验或“进行中”描述。
- 与正文不一致的数据集/模型设定。
- 不可验证的强结论（如“strictly optimal”但无完整证据）。

### 3. 摘要模板（建议）
- 句 1：任务和痛点。
- 句 2：核心方法（只写最终版本）。
- 句 3：结构增强（P2/SimAM/PConv）。
- 句 4：关键结果（mean±std，主指标 + 效率）。
- 句 5：贡献价值（稳定性/泛化/部署）。

---

## 第二章：Introduction（引言）改稿提纲

### 1. 结构建议（5段）
- 段 1：应用价值与挑战（UAV 小目标）。
- 段 2：现有方法不足（架构改动多，度量问题少）。
- 段 3：你的核心洞察（尺度相关监督失衡）。
- 段 4：方法总览（核心方法层 + 架构增强层）。
- 段 5：贡献点（3~4 条，每条可被实验支撑）。

### 2. 贡献点改写规则
- 每条贡献必须映射一张表/一幅图。
- 不写“首次”除非可严格证实。
- 不把工程模块堆叠写成理论创新。

### 3. 引言里要新增的句子
- “我们将方法贡献与工程增强分开评估，以避免模块耦合造成归因不清。”
- “所有主结果基于多随机种子统计，并遵循统一训练预算。”

---

## 第三章：Related Work（相关工作）改稿提纲

### 1. 分组建议
- UAV/小目标检测（架构路线）。
- IoU/NWD/尺度自适应损失（度量路线）。
- 轻量化与注意力（工程路线）。

### 2. 必须改进点
- 删除不确定来源文献和占位对比方法。
- 每组最后一句写“本工作差异点”。
- 避免“贬低式描述”，改为“边界条件说明”。

### 3. 结尾过渡句模板
- “与现有方法不同，我们关注的是尺度相关监督的一致性，并在同一框架中评估其与轻量化结构的互补性。”

---

## 第四章：Method（方法）改稿提纲

### 1. 章节顺序（建议）
1) Problem Setup 与符号定义  
2) SA-NWD 定义与性质  
3) Hybrid Loss（若保留）  
4) TAL/NMS 扩展（若保留）  
5) 架构增强（P2/SimAM/PConv）  
6) 训练策略（单阶段/两阶段对比）

### 2. 符号和公式规范
- 明确 `s` 的定义（全文只用一种）。
- 明确 `C(s)` 最终形式（正文只保留一种）。
- 定理后必须给“可验证实验命题”。

### 3. 新增“实现映射表”（必须）
- 列：理论变量 | 代码变量 | 默认值 | 所在函数。
- 示例：`C0 ↔ c_base=12.0 ↔ patch_sa_nwd_loss`。

### 4. 方法章节里的风险句式（删除）
- “严格最优”“唯一解”但无完整证明。
- “first”/“state-of-the-art”式绝对化结论。

---

## 第五章：Experiments（实验）改稿提纲

### 1. 先写“公平对比协议”小节
- 数据划分、输入尺寸、训练 epoch、增强策略、优化器、硬件。
- 对所有方法尽量同预算；若不同必须说明原因。

### 2. 主结果表（必须升级）
- 每个方法报告：mAP50、mAP50:95、Precision、Recall、F1。
- 同时报告：Params、FLOPs、FPS、Latency、ModelSize。
- 全部关键行改为 mean±std（>=3 seeds）。

### 3. 消融设计（建议两条链）
- 方法链：Baseline → +SA-NWD(loss) → +TAL → +NMS。
- 架构链：Baseline → +P2 → +SimAM → +PConv。
- 目的：把“方法贡献”和“结构贡献”分离。

### 4. 新增分析图（至少 3 张）
- 梯度幅值 vs 目标尺度（IoU/CIoU/NWD/SA-NWD）。
- 训练稳定性曲线（single-stage vs warm-up）。
- 小目标分桶结果图（tiny/small/medium）。

### 5. 统计显著性建议
- 报告 mean±std + 相对提升（%）。
- 可补充 t-test 或 bootstrap 置信区间（加分项）。

### 6. 失败案例分析（必须）
- 误检类型：背景纹理干扰、遮挡、密集小目标。
- 漏检类型：极低对比度、边缘目标、过小目标。
- 每类给 1~2 张可视化和简要原因。

---

## 第六章：Discussion（讨论）改稿提纲

### 1. 建议新增小节
- 方法为何有效：从监督信号角度解释。
- 何时失效：极端尺度、场景偏移、标注噪声。
- 工程价值：部署速度与模型大小权衡。

### 2. 局限性写法（建议直接加）
- 数据规模与类别覆盖有限。
- 仍依赖超参数选取（`C0, k, alpha`）。
- 对跨域数据需额外验证。

---

## 第七章：Conclusion（结论）改稿提纲

### 1. 必须包含
- 你最终采用的方法版本（一句话明确）。
- 关键结果（真实数值，非占位）。
- 贡献边界（理论 + 工程分别表述）。

### 2. 未来工作建议
- 旋转框/多目标跟踪/分割扩展。
- 学习型带宽或自适应参数策略。
- 跨数据集和跨域鲁棒性验证。

---

## 附录与补充材料提纲

1. 完整超参数表（训练/验证/推理）  
2. 多随机种子原始结果  
3. 额外可视化（成功/失败）  
4. 复现实验命令（脚本路径 + 参数）  
5. 理论推导细节（主文简化版对应）

---

## 最终提交前 15 项自检清单

1. 摘要、引言、实验中的数据集描述是否一致。  
2. 模型版本（n/m）是否一致。  
3. `C(s)` 公式在全文是否唯一。  
4. 代码默认值与论文表述是否一致。  
5. BiFPN 是否在“最终方法”中口径一致。  
6. 所有结果是否已替换占位符。  
7. 主结果是否为 mean±std。  
8. 消融是否能分离方法贡献与结构贡献。  
9. 是否给出公平对比协议。  
10. 是否有效率指标完整报告。  
11. 是否有小目标专属指标或分桶分析。  
12. 是否有训练稳定性或收敛分析。  
13. 是否有失败案例和局限性。  
14. 文献是否全部可核验。  
15. 复现命令是否可直接运行。

---

## 建议新增文件（配套）

- `docs/05_公平对比协议.md`：统一训练与评测规范。
- `docs/06_实验结果登记表.md`：记录每个 seed 的原始结果。
- `docs/07_图表与结论映射表.md`：每条结论对应证据图表。
