# 投稿前体检报告

> 生成日期：2026-04-14
> 背景：四个独立 Explore 代理并行审查 + 人工交叉验证
> 新约束：**恒源云服务器已失效（未续费，资源全部丢失）**，剩余工作只能在本地 Mac 上完成或重新租赁 GPU

---

## 一、执行摘要

**一句话结论**：论文主体、代码、引用、内部一致性**全部没问题**；投稿前卡住的只有两件事：

1. 消融表里 Exp D/E/k-sweep/E2/E3/P2-only **5 行占位符**（服务器丢失，这些数据**没保留**）
2. FPS 数字来源不清，摘要注脚是"待重测"

此外作者信息还是占位、几处 TODO/pending meta 标记需要清理。

**关键背景变化**：服务器丢失后，docs/04_投稿改进计划.md 的"TGRS 三数据集长文（4-6 周）"路径已不可行，需要重估目标期刊档次——详见 §六。

---

## 二、四项独立审查摘要

### 2.1 消融实验数据复盘（7 组）

本地幸存的实验全部检查完毕：

| 实验 | Best ep | Best mAP50 | mAP50-95 | 稳定性 | 可信度 |
|------|:-------:|:----------:|:--------:|:-------|:------:|
| baseline | ep266 | **0.95999** | 0.4235 | 高 | ⭐⭐⭐⭐⭐ |
| nwd_only | ep152 | **0.97054** | 0.4091 | 中 | ⭐⭐⭐⭐ |
| **nwd_p2** | ep209 | **0.97806** | 0.4574 | 高 | ⭐⭐⭐⭐⭐ |
| nwd_p2_simam | ep102 | 0.9198 | 0.3880 | **低**（last30 std=0.021，ep102 后衰减） | ⭐⭐ |
| nwd_p2_simam_pconv | ep263 | 0.9451 | 0.4418 | 高 | ⭐⭐⭐⭐ |
| full_improved | ep218 | 0.9627 | 0.4569 | 中 | ⭐⭐⭐⭐ |
| nwd_p2_pconv | ep262 | 0.9505 | 0.4395 | 高 | ⭐⭐⭐⭐ |

**关键校正**：初次 Agent 报告误读出"nwd_p2 best=0.9665"，人工核实 `awk -F, '{print $1,$8}' nwd_p2/results.csv | sort -g -r` 的真实结果是 **ep209=0.97806**，对应论文的 0.9781 ✓。

**nwd_p2_simam 不稳定反而是论据**：该组 "+SimAM 负贡献"的论点正好由训练不稳定性支撑（小数据集过拟合假设），可在 §IV Analysis 补一句定性说明加分。

### 2.2 论文 main.tex / refs.bib 体检

- ✅ 所有 `\label` ↔ `\ref` 闭合
- ✅ 所有 `\cite` ↔ refs.bib 闭合（27 条引用全部存在）
- ✅ 摘要数字与表格数字完全一致（0.9600/0.9705/0.9781、+1.81%、2.66M 参数）
- ❌ 消融表 5 行占位 `--`（见 §三红线）
- ❌ 作者信息、FPS 注脚、pending 标记未清理

### 2.3 nwd.py 代码深度审查

- ✅ **核心公式与论文严格一致**：σ=w/6、C_adapt=C_base(1+k/√S̄)、默认 k=1.0、nwd_min=0.3
- ✅ 数值稳定性：avg_area.clamp(min=1e-4) ✓、exp 下溢保护 ✓、zero-width 保护 ✓
- ⚠️ `nwd_nms()` 默认 k=2.0，与 loss/TAL 的 k=1.0 不一致（L139）
- 🟡 变量 `w2` 实际是 W₂²，命名易误导（L58 应改 `w2_squared`）
- 🟡 DEPRECATED 注释缺"原因"行（L403/488）
- 🟡 `sa_nwd_reverse` 函数缺 docstring 解释与 Fisher 方向对照

### 2.4 数字一致性三方核对

| 数字源 | 状态 | 说明 |
|--------|------|------|
| `paper/main.tex` | ✓ 权威 | 内部完全一致 |
| `README.md` | ✓ 一致 | 与 main.tex 对齐 |
| `docs/CLAUDE.md` | ✓ 一致 | 与 main.tex 对齐 |
| `runs/ablation/*/results.csv` | ✓ 支撑 | 人工核实三主数字对得上 |
| `eval_results.json` | ⚠️ 历史数据 | FPS=59.4 配置不清，参数量 2658868 对应 +NWD+P2 ✓ |
| `ablation_results.json` | ❌ 过时 | imgsz=640 或不同 epoch 的旧评估，应加注释或归档 |
| `k_sensitivity_results.json` | ❌ 空占位 | k=0.5/2.0/3.0 全是 0.0，**从未成功跑过** |

---

## 三、🔴 红线问题（不修不能投）

| # | 问题 | 位置 | 处理 |
|:-:|------|------|------|
| R1 | 作者信息是占位符 `First Author, XXX University` | main.tex L29-33 | 自己填（1 分钟） |
| R2 | **消融表 Table 1 的 5 行占位** — Exp D/E、E2/E3、P2-only 全是 `--` | main.tex L270-278 | **服务器丢了，需要重租 GPU 或改写论文避开** |
| R3 | SOTA 对比表 Table 2 FPS 全是 `--` | main.tex L303-307 | 用 `nwd_p2/weights/best.pt` 本地跑 eval.py 测 FPS（~5 分钟，Mac MPS 或 CPU） |
| R4 | k-sweep 没有真实数据 | `k_sensitivity_results.json` 全 0 | 要么砍掉 k-sweep 折线图，要么重租 GPU 补跑 |
| R5 | Remark 1 "validated in §IV" 虚假声称（Exp D/E 未完成） | main.tex L171-173, L321 | 降级为 "to be validated in future work" 或补实验 |

---

## 四、🟠 投稿前强烈建议修（审稿人会列 minor）

| # | 问题 | 位置 | 处理 |
|:-:|------|------|------|
| W1 | FPS 悬案 `eval_results.json: 59.4` 来源不清 | eval_results.json | 本地重测并写明"evaluated on MacBook M4 MPS" 或 GPU 型号；**论文摘要/表格 FPS 的 GPU 型号必须写清楚** |
| W2 | main.tex 遍布 "pending"、"TODO"、"camera-ready version" 元标记 | main.tex L263/276/312/321 | 全文搜索替换，删除或改为具体内容 |
| W3 | `nwd_nms()` 默认 k=2.0 与 loss/TAL 不一致 | nwd.py L139 | 统一为 k=1.0；如 NMS 不在最终论文里，此项可降为 🟡 |
| W4 | `eval_results.json` 标注过时并归档 | eval_results.json + README | 加头部注释"legacy evaluation, see runs/ablation/ for current results" |
| W5 | `ablation_results.json` 数字过时 | runs/ablation/ | 同上处理，或用 best-epoch 数字重新生成覆盖 |

---

## 五、🟡 润色项（不影响接受）

- nwd.py L403/488 DEPRECATED 注释补"原因"行
- nwd.py L58 `w2` → `w2_squared` 重命名提高可读性
- nwd.py L580+ `sa_nwd_reverse` 补 docstring 说明 Fisher 方向对照
- README.md 消融表加脚注 "validation best epoch, imgsz=1280"
- §IV Analysis 补一句 "SimAM 训练后期退化反而印证过拟合假设" 加分

---

## 六、服务器丢失后的投稿策略重估

### 6.1 剩余资产盘点

**本地有**：
- 7 组消融的**完整训练权重**（baseline/nwd_only/nwd_p2/nwd_p2_simam/nwd_p2_simam_pconv/full_improved/nwd_p2_pconv 的 best.pt + results.csv）
- 论文 main.tex 主体完成
- 所有脚本（scripts/ 共 30+ 个）和模块（ultralytics_modules/）

**彻底失去**：
- Phase 1 的 alpha sweep / Exp D / Exp E / k-sweep 所有结果（从未产出）
- DUT Anti-UAV / AI-TOD 的训练数据和结果（从未启动）
- 服务器上任何中间日志

### 6.2 三条可行路径

**路径 1：接受降级，快投四区（IEEE Access / Sensors / Remote Sensing Letters）**
- 只用现有 7 组实验，砍掉所有 `--` 占位行
- 主论点收缩为："SA-NWD+P2 在私有反无人机数据集上超过基线 1.81%"
- Remark 1 改为"future work"
- 可行性：70-80%（四区对方法新颖性要求较低）
- 时间：清稿 1 周 + FPS 本地重测

**路径 2：重租短期 GPU 补关键实验（TGRS/GRSL 仍有机会）**
- 恒源云 3060 ¥1/h，补 Exp D/E/k-sweep 约 50 小时 = ¥50
- 补完 3 个核心消融即可保住 Remark 1 的 "validated" 声称
- DUT / AI-TOD 如果也补，约 150 小时 = ¥150
- 可行性：GRSL 70%，TGRS 50%
- 时间：再 2-3 周（取决于补多少实验）

**路径 3：完全停损归档**
- 把现有工作存为项目里程碑（7 组消融 + 论文草稿），不投稿
- 用于毕设材料或未来 github 开源
- 可行性：100%（不投稿），但沉没成本全数作废

### 6.3 我的建议

**短期最务实**：路径 2 的轻量版——只花 ¥50 左右补 Exp D/E/k-sweep 三组消融（~50 GPU·h），就能让论文从"一堆 `--` 不能投"变回"Table 1 完整、Remark 1 名实相符"，投 IEEE GRSL 或 Sensors 均可行。

**不建议**：路径 1 的"完全不补实验"版本——表格里留 5 行占位投出去审稿人会直接拒。

---

## 七、下一步优先级（更新版）

```
立即（今天，不花钱）
 ├─ R1 填作者信息（1 分钟）
 ├─ R3 本地跑 eval.py 测 FPS（5 分钟）— 至少给个 Mac M4 的数字占位
 ├─ W4/W5 给过时 json 加头部注释
 └─ 清理 W2 所有 "pending/TODO/camera-ready" 标记

短期（决定路径后）
 ├─ 路径 2：重租 ¥50 GPU 跑 Exp D/E/k-sweep → 填 Table 1 → 投 GRSL
 └─ 路径 1：砍占位行，重写 §IV，投 Access/Sensors

中期（投稿前）
 └─ 润色项（DEPRECATED 注释、变量重命名、README 脚注）
```

---

## 八、关键文件索引

| 文件 | 状态 | 说明 |
|------|------|------|
| `paper/main.tex` | 主体完成，5 行 `--` 占位 | 红线 R2 |
| `runs/ablation/nwd_p2/weights/best.pt` | ✅ 本地保留 | 论文主结果权重 |
| `runs/ablation/*/results.csv` | ✅ 本地保留 | 7 组消融完整训练日志 |
| `ultralytics_modules/nwd.py` | 代码正确，有 🟡 润色项 | 核心实现 |
| `eval_results.json` | 过时，标注处理 | W4 |
| `runs/ablation/ablation_results.json` | 过时，标注处理 | W5 |
| `runs/ablation/k_sensitivity_results.json` | **空占位，k-sweep 从未成功** | R4 |

---

## 九、历史校正说明

本报告过程中 Explore 代理曾误报 `nwd_p2 best mAP50 = 0.9665 at ep271`，导致 "论文数字虚高 1.16%" 的误警。**人工复核 `runs/ablation/nwd_p2/results.csv` 第 8 列排序后证实 ep209=0.97806，论文声称的 0.9781 与实际 results.csv 完全吻合**。此记录保留以备未来参考：对 csv 数字类结论，必须亲自验证，不能完全依赖子代理。
