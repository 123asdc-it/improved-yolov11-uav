# 投稿改进计划

> 目标期刊：**IEEE TGRS / IEEE GRSL**（三区）
> 文档最后更新：2026-03-19（第4轮）
> 状态：论文框架完成，DUT Anti-UAV 已下载，AI-TOD v2 已下载，脚本全部就绪

---

## 一、叙事定位（已确定）

**通用极小目标检测方法（SA-NWD），以反无人机检测为主要应用场景。**

| 数据集 | 角色 | 状态 |
|--------|------|------|
| 私有集（559张，学长提供） | 方法动机来源 + 消融实验 | ✅ 主实验完成，补充消融运行中 |
| DUT Anti-UAV（10K张） | 跨数据集验证 #1，反无人机场景 | ✅ 已下载，脚本就绪，待转换 |
| AI-TOD v2（23GB，DOTA部分~18K） | 跨数据集验证 #2，通用极小目标 | ✅ 已下载，脚本就绪，待解压转换 |

私有集论文里写：*"The dataset was provided by a collaborating research group and is not publicly available."* 不需要 `\cite{}`。

---

## 二、当前工作评估

### 有效贡献（有实验数据）

| # | 贡献 | 新颖性 | 实验支撑 |
|---|------|:-----:|:-------:|
| 1 | SA-NWD 自适应 C 设计（标注噪声正则化动机） | 中高 | 有（+1.05%，私有集） |
| 2 | P2 小目标检测头集成 | 低 | 有（+0.76%，私有集） |
| 3 | G_IoU(s) ∝ s⁻² 梯度分析（Theorem 1） | 中 | 理论 |
| 4 | Exp D/E 对比消融（C 方向验证） | 高 | 待填（运行中） |
| 5 | k 超参数敏感性扫描 | 低 | 待填（运行中） |
| 6 | DUT Anti-UAV 跨数据集验证 | 中 | 待跑（脚本就绪） |
| 7 | AI-TOD 跨数据集验证 | 高（NWD 原文基准） | 待跑（脚本就绪） |

### 已证伪/归档的贡献

| # | 内容 | 结果 | 处理 |
|---|------|------|------|
| — | Fisher-Guided CIoU | 0.827~0.937，全低于基线 | 代码加 DEPRECATED，脚本归档 |
| — | SA-NWD-TAL 微调（从收敛权重） | 震荡崩溃 | 归档 |
| — | Theorem 2（Fisher 等变最优推导） | 推导有跳步，与代码方向相反 | 删除，改为正则化叙述 |

---

## 三、实验全景（按执行顺序）

### Phase 1：私有集补充消融（服务器运行中，~3天）

| 实验 | 脚本 | 说明 |
|------|------|------|
| alpha=0.3/0.7 | run_alpha_0p3/0p7.py | hybrid 权重消融 |
| **Exp D** | run_nwd_fixed.py | Fixed NWD k=0，核心消融 |
| **Exp E** | run_nwd_reverse.py | Reverse C，方向验证 |
| k=0.5/2.0/3.0 | run_k_sensitivity.py | k 扫描 |

### Phase 2：私有集补充实验（Phase 1 完成后，~5天）

| 实验 | 脚本 | 说明 |
|------|------|------|
| E_p2only | run_p2_only.py | P2 head 独立贡献 |
| E2/E3 | run_tal_ablation.py | Loss-only / TAL-only |
| E4 | run_asanwd_compare.py | Asa-NWD batch-level 对比 |
| E1 | run_working_point_log.py | W₂/C 分布统计 |
| E5 | run_nwd_nms.py | NWD-NMS 验证 |
| E6 | — | 私有集 seed=1,2 |
| E_fps | eval.py | FPS 重测（nwd_p2/best.pt） |

### Phase 3：DUT Anti-UAV（脚本就绪，待转换，~4天）

**本地操作（5分钟）：**
```bash
python scripts/convert_dut_to_yolo.py
# 输出: datasets/dut/  (约10GB)
```

**服务器操作（~32h）：**
```bash
rsync -avz datasets/dut/ root@server:/root/drone_detection/datasets/dut/
rsync -avz scripts/ root@server:/root/drone_detection/scripts/
screen -S dut_queue
bash scripts/run_dut_queue.sh 2>&1 | tee logs/dut_queue.log
```

| 实验 | 脚本 | 说明 |
|------|------|------|
| dut_baseline | run_dut_baseline.py | YOLOv11n + CIoU |
| dut_nwd_fixed | run_dut_nwd_fixed.py | + NWD k=0 |
| dut_sanwd | run_dut_sanwd.py | + SA-NWD k=1.0 |
| dut_sanwd_p2 | run_dut_sanwd_p2.py | + SA-NWD + P2（最终方法） |

### Phase 4：AI-TOD（脚本就绪，待解压转换，~7天）

**本地操作：**
```bash
# 1. 解压（文件名以实际为准）
cd datasets && unzip aitod_v2.zip

# 2. 转换（过滤 xView，只用 DOTA 部分 ~18K 张）
python scripts/convert_aitod_to_yolo.py \
    --src datasets/aitod_raw \
    --dst datasets/aitod \
    --filter-xview
```

**服务器操作（~5天）：**
```bash
rsync -avz datasets/aitod/ root@server:/root/drone_detection/datasets/aitod/
screen -S aitod_queue
bash scripts/run_aitod_queue.sh 2>&1 | tee logs/aitod_queue.log
```

| 实验 | 脚本 | imgsz | 说明 |
|------|------|-------|------|
| E7 dry run | run_aitod_queue.sh 内置 | 800 | 1 epoch，确认不 OOM |
| E8 aitod_baseline | run_aitod_baseline.py | 800 | YOLOv11n + CIoU |
| E9 aitod_nwd_fixed | run_aitod_nwd_fixed.py | 800 | + NWD k=0 |
| E10 aitod_sanwd | run_aitod_sanwd.py | 800 | + SA-NWD k=1.0 |
| E11 aitod_sanwd_p2 | run_aitod_sanwd_p2.py | 800 | + SA-NWD + P2 |
| E12 aitod_p2only | run_aitod_p2only.py | 800 | + P2 only |

---

## 四、论文结构（调整后）

```
§IV Experiments
  A. Datasets and Metrics
     - 私有集（动机，559张，极小目标，学长提供，不公开）
     - DUT Anti-UAV（公开，10K张，反无人机，IEEE TITS 2022）
     - AI-TOD（公开，~18K张，通用极小目标，ICPR 2021）
  B. Implementation Details
  C. Ablation Study（私有集，消融完整）
  D. Cross-Dataset Generalization
     Table: Baseline / NWD k=0 / SA-NWD / SA-NWD+P2 × 三个数据集
  E. Analysis（k-sweep折线图，Grad-CAM）
```

---

## 五、待完成 Must（不做不能投）

### M1. 填入实验数据
- [ ] Exp D/E 结果 → 消融表
- [ ] k-sweep → k vs mAP50 折线图（generate_paper_figs.py）
- [ ] alpha sweep → 确认 alpha=0.5 最优
- [ ] E2/E3/E_p2only → 消融表
- [ ] FPS 重测（E_fps）→ 更新摘要注脚和表格
- [ ] DUT Anti-UAV 结果 → §IV-D 表格
- [ ] AI-TOD 结果 → §IV-D 表格

### M2. 数据集说明
- [x] 私有集：写 "provided by a collaborating research group, not publicly available"
- [ ] DUT Anti-UAV：refs.bib 加条目，论文加 `\cite{}`
- [x] AI-TOD：已有 `\cite{wang2022aitod}`

### M3. 投稿前检查
- [ ] 替换作者信息（姓名/学校/邮箱）
- [ ] 所有 `--` 占位数字替换为实际结果
- [ ] Cover Letter（贡献3句 + 适合期刊理由 + 无一稿多投声明）

---

## 六、时间线

```
现在
 ├─ 本地：python scripts/convert_dut_to_yolo.py（5分钟）
 ├─ 本地：解压 AI-TOD，python scripts/convert_aitod_to_yolo.py（~30分钟）
 ├─ rsync 到服务器，启动 DUT 队列
 │
Week 1
 ├─ Phase 1 完成 → 填消融表 Exp D/E/k-sweep/alpha
 ├─ Phase 3 运行中（DUT，~32h）
 │
Week 2
 ├─ Phase 2 完成 → 消融表全部填满
 ├─ Phase 3 完成 → DUT 结果填入论文
 ├─ Phase 4 启动（AI-TOD，~5天）
 │
Week 3
 ├─ Phase 4 完成 → AI-TOD 结果填入论文
 ├─ 论文 §IV 重构为三数据集结构
 ├─ Grad-CAM 可视化
 │
Week 4-6
 ├─ 论文全文整合，投稿前自检
 └─ 投稿 IEEE TGRS（长文，三数据集完整后）
     或先投 IEEE GRSL（短文，Phase 3 完成后即可）
```

---

## 七、决定记录

| 决定 | 内容 | 时间 |
|------|------|------|
| 不使用 VisDrone | 任务不匹配（俯拍地面） | 2026-03-19 |
| 删除 Theorem 2 | 推导有跳步，改为正则化叙述 | 2026-03-19 |
| Fisher-CIoU 归档 | 所有实验低于基线 | 2026-03-19 |
| k-sweep 不加消融表 | 以折线图呈现于 Analysis 小节 | 2026-03-19 |
| AI-TOD v2 过滤 xView | 只用 DOTA 部分（~18K），无需 xView 授权 | 2026-03-19 |
| 私有集定位调整 | 从"主实验"降为"动机来源+消融"，公开集为核心说服力 | 2026-03-19 |
| DUT Anti-UAV 为第一跨数据集 | 场景最贴合（反无人机），先跑 | 2026-03-19 |
| 投稿路线 | TGRS 长文（三数据集完整后）；可先投 GRSL 短文（DUT 完成后） | 2026-03-19 |

---

## 八、目标期刊可行性

| 期刊 | 区 | 可行性 | 前提条件 |
|------|----|:------:|---------|
| IEEE GRSL | 3区 | **高**（Phase 3 完成后） | 短文5页，DUT 结果 + 私有集消融 |
| IEEE TGRS | 3区 | **高**（Phase 4 完成后） | 长文，三数据集，消融完整 |
| IEEE Access | 4区 | 高（M1+M2 完成后） | 影响力较低 |
