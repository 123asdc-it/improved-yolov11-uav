# 投稿改进计划

> 目标期刊：**IEEE TGRS / IEEE GRSL**（三区）
> 文档更新：2026-03-19
> 状态：主实验完成，消融数据待补，公开数据集实验规划中

---

## 一、当前工作评估

### 有效贡献（有实验数据）

| # | 贡献 | 新颖性 | 实验支撑 |
|---|------|:-----:|:-------:|
| 1 | SA-NWD 自适应 C 设计（标注噪声正则化动机） | 中高（设计+消融验证） | 有（+1.05%，私有数据集） |
| 2 | P2 小目标检测头集成 | 低（已有方法组合） | 有（+0.76%，私有数据集） |
| 3 | G_IoU(s) ∝ s⁻² 梯度分析 | 中（改了术语，Theorem 1 成立） | 理论，无直接实验 |
| 4 | Exp D/E 对比消融（C 方向验证） | 高（系统化方向验证，先前未见） | 待填（实验进行中） |
| 5 | k 超参数敏感性扫描 | 低（标准做法） | 待填 |

### 已证伪/归档的贡献

| # | 内容 | 结果 | 处理 |
|---|------|------|------|
| — | Fisher-Guided CIoU | 0.827~0.937，全低于基线 | 代码加 DEPRECATED，脚本归档 |
| — | SA-NWD-TAL 微调（从收敛权重） | 震荡崩溃 | 归档 |
| — | Theorem 2（Fisher 等变最优推导） | 推导有跳步，与代码方向相反 | 删除，改为正则化叙述 |

### 并发工作对比

| 论文 | 方法 | 我们的区别 |
|------|------|----------|
| SMTrack (arXiv:2508.14607) | batch 均值启发式 adaptive NWD | per-object 自适应 + 方向消融验证 |
| SAL (arXiv:2511.09891) | scale-aware IoU 重加权 | 用 Wasserstein 距离，不同路线 |
| RFWNet (arXiv:2503.00545) | CIoU+NWD hybrid，固定归一化 | C 自适应，有消融对比 |

---

## 二、已完成项 ✅

| 问题 | 修改内容 |
|------|---------|
| Theorem 2 跳步推导 | 删除，改为正则化视角 + Design Validation remark |
| 摘要"stronger gradients" | 改为"scale-adaptive regularization" |
| Introduction/Related Work 声称唯一最优 | 改为"annotation-noise regularization 动机 + 实验验证" |
| Theorem 1 术语"Fisher information" | 改为"expected squared gradient G_IoU" |
| TAL 公式 α 符号冲突 | TAL 改用 γ/δ，与混合损失的 α 区分 |
| refs.bib 3 条假引用 | 删除（example2023drone/cfinet/clustered） |
| BiFPN 在 abstract/related work 正面描述 | 删除，改为"消融证实负贡献" |
| Future Work 引用 VisDrone | 改为 Anti-UAV / Det-Fly |
| 消融表 BiFPN 数据矛盾 | 重组表格，加 Exp D/E 占位行 |
| SOTA 表假引用/占位符 | 改为"loss function comparison"，加 NWD 固定 C 行占位 |
| nwd.py Fisher 函数 | 加 DEPRECATED 注释 |
| `__init__.py` | 新增 sa_nwd_reverse / patch_sa_nwd_loss_reverse 导出 |

---

## 三、进行中（服务器队列）

当前新队列（`run_new_queue.sh`）串行运行，约 42h，实验全部使用 nwd_p2 架构：

| 实验 | 脚本 | 输出目录 | 说明 |
|------|------|---------|------|
| alpha=0.3 | run_alpha_0p3.py | nwd_p2_alpha0p3/ | hybrid 权重消融 |
| alpha=0.7 | run_alpha_0p7.py | nwd_p2_alpha0p7/ | hybrid 权重消融 |
| **Exp D** | run_nwd_fixed.py | nwd_p2_fixed_c/ | Fixed NWD (k=0)，核心消融 |
| **Exp E** | run_nwd_reverse.py | nwd_p2_reverse_c/ | Reverse C，方向验证 |
| k=0.5 | run_k_sensitivity.py | nwd_p2_k0p5/ | k 扫描 |
| k=2.0 | run_k_sensitivity.py | nwd_p2_k2p0/ | k 扫描 |
| k=3.0 | run_k_sensitivity.py | nwd_p2_k3p0/ | k 扫描 |

k=1.0 已有（nwd_p2，mAP50=0.9781），不重跑。

---

## 四、待完成 Must（不做不能投）

### M1. 填入实验数据

实验结果出来后：
- [ ] Exp D 结果 → 消融表 Fixed NWD 行
- [ ] Exp E 结果 → 消融表 Reverse C 行
- [ ] SOTA 表 NWD（固定 C）行 = Exp D 结果
- [ ] k-sweep 结果 → 生成 k vs mAP50 折线图（`generate_paper_figs.py`）→ 加入论文 Analysis 小节

### M2. 数据集来源引用

- [ ] 确认数据集出处（学长论文 / 自采）
- [ ] 在论文 §IV Dataset 补 `\cite{}` 引用
- [ ] 若自采，补充采集方式（设备、场景、标注协议）

---

## 五、待完成 P2（影响投稿质量，投三区建议补）

### P2-A. 公开数据集跨数据集验证

**状态**：待调研
**背景**：当前只有私有数据集（559 张），审稿人会追问泛化性。需补充至少一个公开数据集上的实验。

**候选数据集**（均为检测无人机本身的场景）：

| 数据集 | 来源 | 规模 | 标注类型 | 说明 |
|--------|------|------|---------|------|
| Anti-UAV | TPAMI 2022（refs.bib 已有条目） | ~318 序列 | bbox + 红外/RGB | 需确认是否有单帧 bbox 格式 |
| Det-Fly | 2021 | ~10k 帧 | bbox | RGB，单类飞行小目标 |
| Drone-vs-Bird | ECCV Workshop | ~77k 帧 | bbox | 二分类，目标极小 |

**任务**：
- [ ] 调研三个数据集的标注格式（需 bbox，非 mask/track-only）和数据规模
- [ ] 确定一个，下载并转换为 ultralytics YOLO 格式
- [ ] 服务器：Baseline + SA-NWD(Ours)，各 300ep，seed=0
- [ ] 论文 §V 新增"Cross-Dataset Generalization"小节（1~2 段 + 一张表）
- 预计工作量：调研 0.5 天，转换 0.5 天，实验 1 天，写作 0.5 天

### P2-B. Limitation 段

- [ ] 论文 §V 或 Conclusion 后补充 Limitation：数据集规模（391 张训练集）、单一场景、待跨数据集验证
- 1~2 句即可，诚实说明，避免审稿人当作问题追问

---

## 六、决定记录

| 决定 | 内容 | 时间 |
|------|------|------|
| 不使用 VisDrone | 任务不匹配：VisDrone 从无人机俯拍地面目标，本文检测无人机本身 | 2026-03-19 |
| 删除 Theorem 2 | 推导有跳步（C∝s→C∝√s 无根据），与代码实现方向相反，改为正则化叙述 | 2026-03-19 |
| Fisher-CIoU 归档 | 所有实验低于基线，SA-NWD 与 Fisher-CIoU 方向相反导致训练信号混乱 | 2026-03-19 |
| k-sweep 不加消融表 | k-sweep 以折线图形式呈现于 Analysis 小节，消融表只放 Exp D/E 核心发现 | 2026-03-19 |

---

## 七、目标期刊可行性评估（当前状态）

| 期刊 | 区 | 当前可行性 | 前提条件 |
|------|----|:--------:|---------|
| IEEE GRSL | 3区 | **高**（M1/M2 完成后） | 短文（5页），消融完整，数据集来源清楚 |
| IEEE TGRS | 3区 | 中（需补 P2-A） | 长文，需跨数据集验证提升说服力 |
| IEEE Access | 4区 | 高（M1/M2 完成后） | 私有数据集可接受，影响力较低 |
| CVPR/ICCV | 顶会 | 低 | 理论深度和实验规模需大幅加强 |

**建议投稿路线**：M1+M2 完成后先投 **IEEE GRSL**（短文，周期快）；P2-A 完成后同步准备 TGRS 长文版本。
